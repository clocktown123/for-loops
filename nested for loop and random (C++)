#include<iostream>
#include<Windows.h>
using namespace std;

int main() {
	for (int i = 0; i < 1000001; i++) {
		int num = rand() % 20;
		for (int j = 0; j < num ; j++)
			cout << " ";
		cout << "h";
		Sleep(1);
	}
}
